setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
    worker: Dockerfile
release:
  command:
    - flask db upgrade
  image: worker
run:
  web: gunicorn --bind 0.0.0.0:$PORT --log-level=debug wsgi:app
  worker: python worker.py